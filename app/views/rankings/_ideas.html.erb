<strong>Ideas:</strong>

<ul id="ideas">
  Rank according to: <%= @criterion.title if @criterion %>
  <%= render @criterion.ideas if @criterion %>
</ul>
<%= sortable_element 'ideas', :url => sort_idea_url(:criterion) %>

<%= render 'ideas/form', :idea => Idea.new %>
