<%= form_for criterion ||= @criterion do |f| %>
  <%= f.error_messages %>
  <%= f.text_field :title, :size => 70 %>
  <%= f.submit %>
  <% if criterion.title %> 
    <%= link_to "Destroy", criterion, :confirm => 'Are you sure?', :method => :delete %>
  <% end %>
<% end %>
