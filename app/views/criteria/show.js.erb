$("ideas").update("<%= escape_javascript(render(@criterion.ideas)) %>");
$("ideas_sort").update("<%= escape_javascript(sortable_element 'ideas', :url => sort_idea_url(@criterion)) %>");
new Effect.Highlight("ideas", {duration: 2});
